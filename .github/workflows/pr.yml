name: CI on Pull Request
on:
  pull_request:

permissions:
  contents: write
  id-token: write
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Gate-keeper job: Only allow workflow to proceed if PR is from same repository (not a fork)
  check-fork:
    runs-on: ubuntu-latest
    if: github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - name: PR is from same repository
        run: echo "PR is from the same repository, proceeding with workflow"

  ci:
    needs: check-fork
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Run CI Steps
        uses: ./.github/actions/ci-steps

  test-multi-python:
    needs: check-fork
    name: Test Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # 2025-10-14: Commented out 3.14 - Waiting for github to update their 3.14 build to a later version without union type issue
        python-version: ["3.13"]
        # python-version: ["3.13", "3.14"]
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Run multi-Python tests
        uses: ./.github/actions/multi-python-tests
        with:
          python-version: ${{ matrix.python-version }}
